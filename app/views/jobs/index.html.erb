<h1>All Jobs</h1>

<ul>
  <% @jobs.each do |job| %>
  <li>
    <%= link_to job.title, job_path(job) %>
  </li>
  <% end %>
</ul>

<%= link_to "Create a New Job posting", new_job_path, class: "new_job" %>
